<div class="min-h-full px-4 py-16 sm:px-6 sm:py-24 md:grid md:place-items-center lg:px-8">
  <div class="max-w-max mx-auto">
    <main class="sm:flex">
      <p class="text-4xl font-extrabold text-blue-600 sm:text-5xl"><%= status || 500 %></p>
      <div class="sm:ml-6">
        <div class="sm:border-l sm:border-gray-200 sm:pl-6">
          <h1 class="text-4xl font-extrabold text-gray-900 tracking-tight sm:text-5xl"><%= message || 'Algo salió mal' %></h1>
          <p class="mt-1 text-base text-gray-500">
            <% if (error && error.stack && process.env.NODE_ENV === 'development') { %>
              <pre class="mt-4 p-4 bg-gray-100 rounded-md overflow-auto"><code><%= error.stack %></code></pre>
            <% } else { %>
              Lo sentimos, algo ha fallado. Intenta nuevamente más tarde o contacta soporte si el problema persiste.
            <% } %>
          </p>
        </div>
        <div class="mt-10 flex space-x-3 sm:border-l sm:border-transparent sm:pl-6">
          <a href="/" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
            Volver al inicio
          </a>
          <a href="/contact" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-blue-700 bg-blue-100 hover:bg-blue-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
            Contactar soporte
          </a>
        </div>
      </div>
    </main>
  </div>
</div>
