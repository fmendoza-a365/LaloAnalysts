<div class="bg-white shadow rounded-lg">
  <div class="px-4 py-5 sm:px-6 border-b border-gray-200 flex items-center justify-between">
    <div>
      <h3 class="text-lg leading-6 font-medium text-gray-900">Asesores · Carga de Datos</h3>
      <p class="mt-1 text-sm text-gray-500">Solo formato Excel (.xlsx, .xls) con las cabeceras exactas. Para ver indicadores, accede al módulo "Asesores" en el menú lateral.</p>
    </div>
  </div>

  <div class="px-4 py-5 sm:p-6">
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
      <div class="lg:col-span-1">
        <h4 class="text-sm font-semibold text-gray-800 mb-2">Cabeceras requeridas</h4>
        <ul class="list-disc list-inside text-sm text-gray-700">
          <% headers.forEach(h => { %>
            <li><code class="bg-gray-100 px-1 py-0.5 rounded"><%= h %></code></li>
          <% }) %>
        </ul>
        <p class="mt-3 text-xs text-gray-500">El archivo debe contener todas las cabeceras indicadas. Los registros se actualizan por <strong>DNI</strong> (upsert). Puedes descargar una plantilla en Excel para asegurar el formato correcto.</p>
        <div class="mt-3 flex items-center gap-2">
          <a href="/admin/asesores/plantilla" class="inline-flex items-center px-3 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50">Descargar plantilla</a>
        </div>
        <div class="mt-6">
          <h5 class="text-sm font-semibold text-gray-800 mb-2">Editar cabeceras</h5>
          <form action="/admin/asesores/cabeceras" method="POST" class="space-y-2">
            <label for="headersRaw" class="block text-xs font-medium text-gray-700">Una cabecera por línea</label>
            <textarea id="headersRaw" name="headersRaw" rows="8" class="w-full border border-gray-300 rounded-md p-2 text-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500"><%= headers.join('\n') %></textarea>
            <div class="flex justify-end">
              <button type="submit" class="inline-flex items-center px-3 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-green-600 hover:bg-green-700">Guardar cabeceras</button>
            </div>
          </form>
        </div>
      </div>
      <div class="lg:col-span-2">
        <form action="/admin/asesores/carga" method="POST" enctype="multipart/form-data" class="space-y-4">
          <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
            <div>
              <label for="tipoDoc" class="block text-sm font-medium text-gray-700">Tipo de documento</label>
              <select required id="tipoDoc" name="tipoDoc" class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm px-3 py-2 focus:outline-none focus:ring-blue-500 focus:border-blue-500 text-sm">
                <option value="DNI">DNI (8 dígitos)</option>
                <option value="CE">Carné de Extranjería (10 dígitos)</option>
              </select>
              <p class="mt-1 text-xs text-gray-500">Se validará que la columna <strong>DNI</strong> tenga longitud 8 (DNI) o 10 (CE) y solo números.</p>
            </div>
            <div class="sm:col-span-2">
              <label for="archivo" class="block text-sm font-medium text-gray-700">Archivo Excel (.xlsx/.xls)</label>
              <input required accept=".xlsx,.xls" type="file" name="archivo" id="archivo" class="mt-1 block w-full text-sm text-gray-900 border border-gray-300 rounded-lg cursor-pointer bg-gray-50 focus:outline-none">
            </div>
          </div>
          <div class="flex justify-end">
            <button type="submit" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none">Cargar</button>
          </div>
        </form>
      </div>
    </div>

    <div class="mt-8">
      <h4 class="text-sm font-semibold text-gray-800 mb-3">Listado reciente</h4>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">DNI</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Genesys</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Apellidos y Nombres</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Supervisor</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Estado</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Turno</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Modalidad</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Pool</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actualizado</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% if (asesores && asesores.length) { %>
              <% asesores.forEach(a => { %>
                <tr>
                  <td class="px-3 py-2 text-sm text-gray-900"><%= a.DNI %></td>
                  <td class="px-3 py-2 text-sm text-gray-700"><%= a.nombreGenesys %></td>
                  <td class="px-3 py-2 text-sm text-gray-700"><%= a.apellidosNombres %></td>
                  <td class="px-3 py-2 text-sm text-gray-700"><%= a.supervisor %></td>
                  <td class="px-3 py-2 text-sm">
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full <%= a.estado && a.estado.toLowerCase().includes('activo') ? 'bg-green-100 text-green-800' : 'bg-gray-100 text-gray-800' %>">
                      <%= a.estado || '-' %>
                    </span>
                  </td>
                  <td class="px-3 py-2 text-sm text-gray-700"><%= a.turno || '-' %></td>
                  <td class="px-3 py-2 text-sm text-gray-700"><%= a.modalidad || '-' %></td>
                  <td class="px-3 py-2 text-sm text-gray-700"><%= a.pool || '-' %></td>
                  <td class="px-3 py-2 text-sm text-gray-500"><%= new Date(a.updatedAt).toLocaleString() %></td>
                </tr>
              <% }) %>
            <% } else { %>
              <tr>
                <td colspan="9" class="px-3 py-4 text-center text-sm text-gray-500">No hay registros de asesores.</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
      <div class="mt-4 flex items-center justify-between text-sm text-gray-600">
        <div> Página <%= pagination.page %> de <%= pagination.totalPages %> — <%= pagination.total %> registro(s)</div>
        <div class="space-x-2">
          <% const base = '/admin/asesores?perPage=' + pagination.perPage + '&page='; %>
          <% if (pagination.page > 1) { %>
            <a class="px-3 py-1 border rounded" href="<%= base + 1 %>">Primera</a>
            <a class="px-3 py-1 border rounded" href="<%= base + (pagination.page - 1) %>">Anterior</a>
          <% } %>
          <% if (pagination.page < pagination.totalPages) { %>
            <a class="px-3 py-1 border rounded" href="<%= base + (pagination.page + 1) %>">Siguiente</a>
            <a class="px-3 py-1 border rounded" href="<%= base + pagination.totalPages %>">Última</a>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</div>
